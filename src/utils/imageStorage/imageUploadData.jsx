async function uploadFoundImage(file, itemId, teamId) {
  console.log('file', file);
  console.log('itemId', itemId);
  console.log('teamId', teamId);
  return null;
}  

export { uploadFoundImage };
